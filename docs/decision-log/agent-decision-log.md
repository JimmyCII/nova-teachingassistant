# Agent Decision Log

Significant decisions and their rationale (Project-Playbook §6). Newest first.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-06-21 | **Dual MCP Servers & Agentic Approval Loop** | To prove Day-2 MCP interoperability for the Capstone, we built a second `comms_server` MCP (Gmail + Drive approval sensing). We updated `pedagogy_critic` to *consume* the `curriculum_server` natively via a new `mcp_client.py` over `stdio`. Finally, we wired `weekly_quiz.py` and `request_logger` to form a human-in-the-loop: generated drafts are emailed to Karrie via the Comms MCP, logged as "Pending Approval", and automatically marked "Approved" when she moves the file to the `03_Approved` folder in Google Drive. |
| 2026-06-21 | **Deploy Security: `CONSOLE_TOKEN`** | Cloud Run requires `--allow-unauthenticated` for the web UI to load, exposing the `/ws` socket which spends the Gemini API key. Added a 32-byte HMAC `CONSOLE_TOKEN` enforcement on the WebSocket route, passed securely via the WS subprotocol. |
| 2026-06-21 | **Multi-Agent Orchestration (Pedagogy Critic)** | To meet Capstone requirements and ensure quality, quiz generation was shifted from a one-shot flat tool to a collaborative pipeline. A new `pedagogy_critic.py` agent reviews the drafted quiz against AZ standards and DOK 2 rigor before it is approved. |
| 2026-06-21 | **Curriculum MCP Server** | To meet Capstone requirements, scaffolded `mcp/curriculum_server/server.py` using FastMCP. It exposes `standards://az-math-6` and `dok://webb-model` as resources, and wraps Google Drive upload as a `save_draft` tool. |
| 2026-06-21 | **Quiz output as Word Document (`.docx`)** | Karrie is open to Word but not pure Markdown for this task. Updated `weekly_quiz.py` to use `python-docx` to format the LLM output into a native `.docx` file before uploading to Google Drive. |
| 2026-06-21 | **Quiz format: 4-5 DOK 2 questions** | Replaced the generic 10-question quiz prompt with a highly specific prompt targeting exactly 4-5 DOK 2 level questions to match Karrie's upcoming end-of-year testing strategy. |
| 2026-06-21 | **Voice Console Prompts: Natural Conversation** | Reverted UI buttons from passing hard-coded parameters to simple intent strings (e.g., "Hey Nova, create a weekly quiz") so Nova's required parameters naturally trigger her to ask clarifying questions over voice. |
| 2026-06-21 | **Quizzes & DOK output as Markdown to Drive** (skipping `.xlsx` for now) | Markdown is faster and more reliable for LLMs to generate. We wired `GoogleDriveClient` to guess mimetypes and upload `.md` files natively to Drive. |
| 2026-06-21 | **Request Log CSV instead of Pacing/Mastery backbone (Session 4)** | We built `Nova_Request_Log.csv` as a lightweight short-term memory for Nova. This allowed us to immediately ship the "What did we discuss recently?" feature without the heavy lift of the full Pacing tool. |
| 2026-06-21 | **Flat Voice API Tooling instead of MCP/ADK Orchestrator (Sessions 5 & 7)** | To rapidly prototype the 3 key features (Quizzes, Homework, Group Activities), we wired tools directly into the Gemini Live API schema (`VOICE_TOOLS_API`) in `server.py`, skipping the complex ADK sub-agent routing and MCP servers. |
| 2026-06-20 | **Spiral homework generator built & live-verified** (subagent-driven TDD, 13 tests); default model `gemini-2.5-flash` (2.0-flash had 0 free-tier quota); editable `.xlsx` → Nova's Drive `Homework/<year>/Drafts` shared with Karrie | First real Nova capability shipped end-to-end. OAuth `drive.file`, consented as Nova's account; files share to Karrie via `KARRIE_EMAIL`. Branch `feat/spiral-homework`. |
| 2026-06-20 | **Spiral homework generator = increment 1** (spec'd; Gemini writes the ~50/50 spiral mix → openpyxl `.xlsx` → Nova's Google Drive `Homework/<year>/Drafts`) | Editable Excel keeps Karrie in control of her own approach; Drive is the "home" for generated work, shared with her. Split thinking (LLM) from drawing (deterministic renderer) for testability. |
| 2026-06-20 | **Spiral homework knowledge base built** (style guide + exemplars + templates, local-only) | 2 agents (openpyxl) + a visual PDF pass analyzed 54 weekly Spiral Review files; powers the generator's few-shot. These are box-style weekly worksheets (current+review mix), authored in Excel. |
| 2026-06-20 | **Drive auth = OAuth `drive.file`** (least privilege) for `nova-assistant@example.com` | App only sees files it creates; one-time consent; token gitignored. Local `.xlsx` fallback if Drive fails so work is never lost. |
| 2026-06-20 | **Voice console = FastAPI + Gemini Live API WebSocket bridge** (`web/`), not a pure ADK Runner | Fastest path to a talkable Nova with an audio-reactive orb; it IS the Gemini Live API (ADK-compatible). Cloud Run deploy is the "deploy in ADK/GCP" step (Session 2). |
| 2026-06-20 | **/ws security:** localhost-only default + same-origin **Origin check** (anti-CSWSH) + constant-time **token** via WS subprotocol + URL `#fragment`; `/health` hides the key | Two commit-review passes flagged that the socket spends the API key; hardened so exposure (Cloud/tunnel) is safe and a drive-by site can't hijack it. |
| 2026-06-20 | **Hands-free session + arc-reactor orb** (additive glow, capped radius) | Tap Talk once → continuous conversation (server loops `session.receive()` across turns); additive/capped glow fixed the "box" clipping; conversation seated just under the orb. |
| 2026-06-20 | **Speaker-awareness via prompt** (don't assume speaker is Karrie; learn introduced names in-session) | Achievable now without biometrics; true voice-ID needs a speaker-recognition model (deferred, idea #7). |
| 2026-06-20 | **Persona flavor:** Karrie = Darth Vader on May 4th, "more Vader than Jedi" teacher | Real, characterful detail; Star Wars references about her can lean playful-dark-side (Nova itself stays warm). |
| 2026-06-20 | **Discovered existing TeacherMind v1** in `agent/` during scaffolding; kept ADK `agent/` layout (removed redundant `src/`) | Code already implements the capstone's 12 tools; don't break working ADK-idiomatic structure to fit the playbook's generic `src/`. |
| 2026-06-20 | **Identified the key gap: Nova voice not wired into the agent** (`SYSTEM_PROMPT` + `communication_drafter` are generic) | Bridges the two work streams; made the #1 task in `prompts/session-1-kickoff.md`. |
| 2026-06-20 | **Gitignore all Karrie-derived research** (`docs/karrie_profile/`, photo guide, raw CSV, exploration prompt) | They reference real student-name filenames + IEP/SPED/SEI paths; FERPA + playbook forbid committing PII. Local-only until scrubbed. |
| 2026-06-20 | **Agent persona named "Nova"** (nickname "Vi"); assistant *in Karrie's spirit*, not a clone | Nova = a star at its brightest → her "Star Polisher" value; tasteful Star Wars/Disney nod (her fandoms). The agent named itself. |
| 2026-06-20 | **Corrected owl→Star Wars/Disney** attribution | Owl motif belongs to a co-teacher, not Karrie; her fandoms are Star Wars & Disney (Disney purses). |
| 2026-06-20 | **Verified current enVision sequence**; Statistics fully taught (not "sparse") | Real file mtimes + 2025-26 folder corrected Agents 2/5's inferred ordering. |
| 2026-06-20 | **Repo scaffolded to Project-Playbook**; build paused mid-brainstorming | Plan-before-code; resume via `prompts/session-1-kickoff.md`. |
