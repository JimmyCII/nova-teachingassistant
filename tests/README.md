# tests/

Tests for TeacherMind. **All test data must be synthetic — never real student PII.**

Run: `pytest`. Current tests: `test_grade_tools.py`. Each increment must pass tests + type-check +
lint before commit (Project-Playbook §3). Code under test lives in `agent/`.
